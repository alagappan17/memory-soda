# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

.nx/cache
.nx/workspace-data
.claude/worktrees
.claude/settings.local.json

# Next.js
.next
out

# Environment variables
.env
.env.local
.env*.local

test-output

wiki
# Root-only. A bare `docs` also matched apps/docs (the docs site), which hid the
# whole app from git — and therefore from Nx, which reads the git file list.
/docs
CLAUDE.md
plans
*.tsbuildinfo

# Local tool config
.codex/
